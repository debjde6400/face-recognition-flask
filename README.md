# face-recognition-flask - App for object detection using OpenCV, Flask

To run this server:

- Install packages: `opencv-python`, `numpy`, `pillow` (using `pip` / `conda` `install <package name>`)
- cd to the root directory and `python driver.py`
